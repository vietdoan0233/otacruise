KIDE_EVENT_URL=https://kide.app/fi/events/bd92ef0f-b02f-4a0b-b439-580b4c01bb2a
DRY_RUN=false
HEADLESS=false
KIDE_WATCH_FOREVER=true
KIDE_KEEP_BROWSER_OPEN=true
KIDE_MAX_WAIT_MS=1800000
KIDE_POLL_INTERVAL_MS=1000

# Preferred: connect to a locally authenticated browser started with CDP.
# KIDE_CDP_URL=http://127.0.0.1:9222

# Default: persist the authenticated browser session locally for future runs.
# `.local/` is gitignored; the automation never prints or exports its cookies.
KIDE_PROFILE_DIR=.local/kide-profile

# Alternative session-state file. This is read when set instead of the profile.
# Keep it outside version control and never put credentials in this file manually.
# KIDE_STORAGE_STATE=.local/auth-state.json
PLAYWRIGHT_EXECUTABLE_PATH=

# Optional: fill in both to enable one-time automated sign-in at startup
# instead of signing in by hand in the visible browser. Leaving either one
# blank (the default) keeps manual sign-in, which is the safer option --
# these two values sit in plaintext in this file on this machine. Accepts
# the same email-or-username Kide's own login form does.
# KIDE_ACCOUNT_USERNAME=
# KIDE_ACCOUNT_PASSWORD=

# This file (once you copy it to .env) is gitignored, so real values here
# are never committed. Still, never commit tokens, cookies, or payment data,
# and never paste this file's contents anywhere outside this machine.
